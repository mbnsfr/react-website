import React from 'react';
import { withNamespaces } from 'react-i18next';

const App = ({ t }) => {

    return (
        <>
            Article
        </>
    );
};

export default withNamespaces()(App);
